country_decisions = {
	
	proclaim_anglo_french_union = {
		major = yes
		potential = {
			tag = FRA
			has_country_flag = fra_can_rename_to_union
		}
		allow = {
			is_at_war = no
			is_subject = no
		}
		effect = {
			override_country_name = ANGLO_FRENCH_UNION
			clr_country_flag = fra_can_rename_to_union
		}
		ai_will_do = {
			factor = 1
		}
	}
}