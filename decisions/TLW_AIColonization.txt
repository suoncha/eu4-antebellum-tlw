country_decisions = {

	enable_faster_early_colonization = {
		major = yes

		potential = {
			ai = no
			NOT = { has_global_flag = AB_FASTER_EARLY_COLONIZATION }
			NOT = { is_year = 1445 }
		}

		allow = {
			ai = no
		}
		
		effect = {
			set_global_flag = AB_FASTER_EARLY_COLONIZATION
			custom_tooltip = FASTER_AI_EARLY_COLONIZATION
			hidden_effect = {
				if = {
					limit = {
						technology_group = north_american
					}
					set_global_flag = AB_EARLY_COLONIZATION_PUNIC
				}
				else_if = {
					limit = {
						OR = {
							capital_scope = {
								region = caribbean_sea_region
							}
							technology_group = mesoamerican
						}
					}
					set_global_flag = AB_EARLY_COLONIZATION_MAYA
				}
				else_if = {
					limit = {
						OR = {
							technology_group = andean
							technology_group = south_american
						}
					}
					set_global_flag = AB_EARLY_COLONIZATION_INCA
				}
				every_country = {
					limit = {
						OR = {
							tag = NOR
							tag = ENG
							tag = HOL
							tag = FRC
							tag = ADU
						}
					}
					country_event = {
						id = ab_punic.19
					}
				}
				country_event = {
					id = ab_punic.18
					days = 12775
				}
			}
		}
	}
}